{
  "name": "bakery-hub",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=frontend && cp -r frontend/dist ./public"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
