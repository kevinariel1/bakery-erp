{
  "name": "bakery-hub",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace=frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
